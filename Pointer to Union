#include <stdio.h>

union Student
{
    int roll;
    float marks;
};

int main()
{
    union Student s;
    union Student *ptr = &s;

    ptr->roll = 101;

    printf("Roll = %d\n", ptr->roll);

    return 0;
}
Output
Roll = 101
