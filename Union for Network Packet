#include <stdio.h>

union Packet
{
    int packetId;
    char data[4];
};

int main()
{
    union Packet p;

    p.packetId = 1001;

    printf("Packet ID = %d\n", p.packetId);

    return 0;
}
Output
Packet ID = 1001
