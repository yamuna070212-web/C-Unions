#include <stdio.h>

union Test
{
    int number;
    int *ptr;
};

int main()
{
    int x = 200;

    union Test t;

    t.ptr = &x;

    printf("Value = %d\n", *t.ptr);

    return 0;
}
Output
Value = 200
